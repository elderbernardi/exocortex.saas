# Prompt de handoff — Canvas de Tarefas: consertar o drift do E3 + des-burocratizar o Cockpit

> Cole o bloco abaixo numa sessão nova. Ele é self-contained: carrega o contexto, nomeia o drift, e prescreve o método rigoroso que funcionou no F3.

---

Continue o programa **Canvas de Tarefas** (Exocortex/hermes-webui) em **MODO MULTIAGENTE**, executando a rodada de UX que **conserta o drift do E3**. Antes de agir, leia a memória `[[canvas-tarefas-meta-issue-2026-07-23]]` (que tem todo o histórico F0–F3 + este drift), o **draft do meta issue** (`exocortex.saas/docs/drafts/2026-07-23_issue-canvas-tarefas-meta.md`, unidades E1–E10) e os charters **F1/F4/F5** (`exocortex.saas/docs/plans/2026-07-23_canvas-tarefas/`).

## ONDE ESTAMOS
F0/F1a/F1b/F2/F3 **MERGEADOS + PUSHED** (2026-07-27): fork `elderbernardi/hermes-webui@exocortex/stable` (F3 = `ff832ab5`), exocortex.saas `main` (`1c0f1e0`), umbrella `projetob/master` (`f2c379c`). F3 (Sala viva) **verificado ao vivo** (Playwright: a zona "SALA VIVA" renderiza chip de fase PT-BR + 4 cards; screenshot `f3-sala-viva-cards.png`). Gate T16 = **parcial**: a SOUL conduz o agente lançado, mas ele **narra** o método em vez de escrever `conduct.jsonl` → sem cards sala automáticos (calibração F5 das skills `excrtx-conduct-*`). Contrato `.harness/contracts/exocortex-hermes-webui.md` §(a)–(g). **NÃO tocar prod `127.0.0.1:8787` nem o acervo real.**

## O DRIFT A CONSERTAR (leia com atenção — é o alvo)
A unidade da visão **E3 · Canvas UI** (meta issue) tinha zonas fixas: `Foco/Pronto · Vetor · Microversos · `**`Acervo · Personas`**` · Gaps · Artefatos · `**`Colheita`**` · Next moves`, **alimentadas por E4 (Curador)** que sugere `skills/personas/templates/workflows` com memória de capacidades por microverso. **O que aconteceu:**
- **F1 (MVP Cockpit) cortou o E3** para um subconjunto (só `Foco/Vetor/Microversos/Lacunas/Artefatos`) — meta issue linha 135: *"F1 não depende de E4/E8/E10 completos"*.
- **F2 (= E4/Curador) entregou os DADOS** (`acervo_aplicado[]`, `personas.suggested[]` no canvas) + um render **mínimo numa ilha bolt-on** (`static/canvas-curador.js`, "Sugestões do Curador") — **não** as zonas integradas do E3.
- **Nenhuma fase posterior assumiu "terminar o E3"**: F4 = Colheita/E8 (canoniza de volta, outra coisa); F5 = polish-only ("nenhuma feature nova").
- **Consequências (o que o owner sentiu):** (1) o Cockpit é um **formulário burocrático** — campos free-text editados via ✎, **0 dropdowns**; o microverso é **texto livre**, não dá pra **escolher** entre os 13 existentes; (2) o reuso de acervo/personas/skills ficou **órfão** (só na ilha à parte); (3) **skill-usage de domínio** é implícito — o Curador *sugere* o path, mas *"a sessão lançada CARREGA/usa a skill X"* nunca virou entregável discreto (além do brief citar o path + o runtime do acervo ter as skills).

## A TAREFA (decisões preliminares do owner — reconfirmar no brainstorming)
Uma **rodada de UX que conserta o drift**, em duas frentes que o owner já sinalizou (ele travou "des-burocratizar o formulário" e **inclinou a dobrar o escopo pra incluir o reuso**):
1. **Des-burocratizar o Cockpit**: microverso vira **dropdown dos 13 reais** (`comercial, consultoria, divulgacao-ifsul, ensino, estudio-criativo, estudio-editorial, excrtx, exocortex-dev, exocortex-ops, gabinete, projetob, projeto-d, sales-ai`) via novo forward `GET /api/canvas/microversos` (lê `canvas_store.acervo_root()/micro`, mesmo padrão de `_valid_allow_scopes` no Curador); `vetor`(4)/`intent_type`(8)/`shape`(3) viram **chips**, não texto livre; **colapsar** os campos de método (`done_criteria`/`verification`/`scope`/`assumptions`) atrás de "Detalhes do método ▸"; **liderar com a frase** (o intento) no topo, não um campo "Foco:" rotulado. Alvo: lê como *cartão que o agente preencheu e você nudge*, não *formulário a preencher*.
2. **Terminar o E3**: trazer **Acervo Aplicado + Personas + skills sugeridas** para dentro do Cockpit como **zonas de 1ª classe** (não a ilha à parte), **alimentadas pelo Curador** (não reinventar — `sugerir_itens` já produz isso; integrar o botão "pedir sugestões" e o render `/personas/suggested` + `/acervo_aplicado` ao Cockpit). Decidir o destino da ilha `canvas-curador.js` (absorver vs manter).
3. **Skill-usage de domínio**: decidir/desenhar se e como a sessão lançada *usa* as skills de domínio sugeridas (só citação no brief? carregar explicitamente? escopo F5?).

Ponto de partida já esboçado (design das 6 mudanças + before/after ASCII) está no histórico da sessão anterior; o `renderCockpit` fica em `static/canvas-tarefas.js` (~linha 187).

## MÉTODO (repita o padrão que funcionou no F3)
1. **superpowers:brainstorming** → alinhar escopo com o owner (AskUserQuestion: dobrar-pra-reuso vs só-campos; destino da ilha do Curador; skill-usage) → **spec** em `exocortex.saas/docs/superpowers/specs/YYYY-MM-DD-*.md`.
2. Workflow de design multiagente (leitores paralelos do código real: `canvas-tarefas.js`/`renderCockpit`, `canvas-curador.js`, `canvas_curador.py sugerir_itens`, `canvas_store`, o esquema `microversos`/`personas`/`acervo_aplicado`; charters E3/E4) → 2–3 arquiteturas → sintetizar (best-of + TABELA DE DECISÕES DO OWNER) → **crítica adversarial**.
3. **superpowers:writing-plans** → `PLANO.md` (TDD bite-sized, código completo, prova EX-49 por tarefa, Global Constraints do 00-INDEX verbatim + a emenda do owner sobre regras 3/4).
4. **Revisão adversarial do plano** (workflow, vs código REAL) → **re-verify do fold** → dobrar achados antes do SDD.
5. **SDD via Workflow** (sequencial impl→review→(fix) por tarefa, numa worktree isolada da branch; **NUNCA 2 implementers na mesma branch**) → **revisão de branch inteira no modelo mais capaz** → **fix wave única**.
6. Merge `--no-ff` → mains + push (**só após o owner aprovar o gate**) + **gate ao vivo** (smoke DeepSeek isolado + Playwright).

## GOTCHAS OPERACIONAIS VINCULANTES
- **Zona quente (NÃO tocar):** `static/{ui,messages,sessions,panels,boot}.js`, `static/style.css`, `static/index.html`. **`routes.py` = 0 linhas** (endpoints entram por **forward** em `api/canvas_tarefas.py`). Editáveis (fork-owned, MOD): `static/canvas-tarefas.js`/`.css`, `canvas-dev.html`, `canvas-sala.js`, `canvas-curador.js`, `api/canvas_*.py`. **Emenda do owner (2026-07-26):** o teto de "8 linhas" do routes.py e "zero deps" foram relaxados — o invariante real é **não perturbar o upstream nesquena** (sem build step; dep Python pequeno OK se não tocar tooling de frontend upstream). `zero build step` PERMANECE.
- **Checkout compartilhado (owner roda sessões paralelas nos MESMOS checkouts):** trabalhe SEMPRE em **worktree isolada** em scratchpad (`git worktree add`); branch nova (ex. `collab/canvas-e3-ux`); verifique a branch no **mesmo comando composto** do commit; **NUNCA `git add -A`** (paths explícitos). **Merge via worktree DETACHED no tip do ORIGIN** (`git worktree add --detach <wt> origin/<branch>` → merge → `push origin HEAD:<branch>`) — os main checkouts podem ter **divergido/ter WIP do owner** (aconteceu no F3: exo estava 7 commits atrás, umbrella com dirty). Ver `[[checkouts-compartilhados-sessoes-paralelas]]`.
- **Runner de teste (hermético, sem chave):** `cd <worktree> && /home/elder/projetos/projetob/hermes-webui/.venv/bin/python -m pytest <arquivos> -q` (PYTHONPATH = a worktree). Seams: `SALA_ENABLE`, `_INJECTED`, `CANVAS_LLM_CMD`, `CURADOR_*`. Lint de contrato: `UMBRELLA_ROOT=<umbrella-wt>`. Playwright via `mcp__playwright__*`.
- **Smoke DeepSeek ao vivo (isolado — GOTCHA):** `HERMES_HOME` isolado = `cp ~/.hermes/config.yaml` + override `model.provider=deepseek` + `model.default=deepseek-v4-pro` + **`model.base_url=https://api.deepseek.com/v1`** + **`model.api_mode=openai_chat_completions`** (⚠ sem sobrescrever o `model.base_url`, ele herda a **Zen** do config prod e dá **401 com cara de erro Anthropic**) + `providers.deepseek.base_url` + env `DEEPSEEK_API_KEY` (de `databrain/.env`). `SOUL.md` = `SOUL_SEED.md` compilado do exocortex. `ACERVO` isolado = `global/tools/harness` + `global/templates` copiados do acervo real. Server: `server.py` com `HERMES_WEBUI_PORT=8792 SALA_ENABLE=1` + venv do hermes-agent + `PYTHONPATH=<worktree>`. **Prod :8787 (verifique o pid antes/depois) e o acervo real INTOCADOS.** No navegador: `http://127.0.0.1:8792/static/canvas-dev.html` → 🗺️ Canvas → clicar num card abre o **Cockpit** → a zona nova aparece lá.
- **Governança (COLLAB 3-repos):** tocar contrato SSE/canvas OU adicionar endpoint = COLLAB (contrato + change-record no **umbrella**, MOD no **fork** `EXOCRTX_MODIFICATIONS.md`, ADR/skills no **exocortex** — commits SEPARADOS). O novo `GET /api/canvas/microversos` é **aditivo** (nota no contrato, não breaking).
- **Alimentar-se do Curador, não reinventar:** o reuso integrado deve consumir `sugerir_itens`/`buscar_acervo` (F2, já shippado) + `acervo_aplicado[]`/`personas.suggested[]` no canvas. As skills de MÉTODO (`excrtx-conduct-*`, F3) são separadas das skills de DOMÍNIO (sugeridas pelo Curador).

## OWNER-GATED / abertos (registre, não bloqueiam a rodada)
Gate ao vivo do T16 do F3 (conduct.jsonl real com cards sala) = calibração F5 das skills conduct. F4 (Colheita) e F5 (polish/calibração) seguem abertos. A decisão "dobrar o escopo pra incluir reuso" é preliminar — reconfirmar no brainstorming.

**Comece lendo a memória + o meta issue draft (E1–E10) + F1/F4/F5 charters + o esboço de design das 6 mudanças, e proponha o brainstorming/escopo ANTES de codar.**
