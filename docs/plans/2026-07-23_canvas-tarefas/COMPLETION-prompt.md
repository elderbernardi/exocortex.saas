# Prompt de conclusão — levar o Canvas de Tarefas (#130) a GA

> Cole o bloco abaixo numa sessão nova. É o prompt-mestre com **goal de concluir a empreitada toda**: dirige o programa Canvas de Tarefas até fechar o meta issue #130 com veredito GO, uma fase gated por vez, no método multiagente que já entregou F0–F3.

---

## GOAL (norte da empreitada)
**Concluir o programa Canvas de Tarefas (meta issue elderbernardi/exocortex.saas#130) até GA:** toda unidade restante da visão (E1–E10) concluída, o **gate T16 do F3 fechado AO VIVO** (uma sessão real conduzida gera cards na Sala — não só injetado), **auditoria interativa G0–G5 com veredito GO** (persona executivo não-técnico faz a jornada inteira: frase → sala → execução → colheita → receita), docs atualizados, `sources.lock` re-pinado, e **#130 fechada com sumário de evidências**. Trabalhe em MODO MULTIAGENTE, **uma fase gated por vez**, sem pular gate nem enfraquecer verificação.

## ESTADO (o que já está feito e no ar)
F0/F1a/F1b/F2/F3 **MERGEADOS + PUSHED** (2026-07-27): fork `elderbernardi/hermes-webui@exocortex/stable` (F3=`ff832ab5`), `exocortex.saas@main`(`1c0f1e0`), umbrella `projetob@master`(`f2c379c`). Contrato `.harness/contracts/exocortex-hermes-webui.md` §(a)–(g). F3 verificado ao vivo (Playwright — a zona "Sala viva" renderiza chip de fase + 4 cards). **NÃO tocar prod `127.0.0.1:8787` nem o acervo real.** Comece lendo a memória `[[canvas-tarefas-meta-issue-2026-07-23]]`, o draft do meta issue (`docs/drafts/2026-07-23_issue-canvas-tarefas-meta.md`, unidades E1–E10 + as 10 provas P1–P11) e os charters `docs/plans/2026-07-23_canvas-tarefas/F4-CHARTER.md` + `F5-CHARTER.md`.

## O QUE FALTA (mapa de conclusão — 3 fases gated, nesta ordem, salvo decisão do owner)

### Fase C1 · Consertar o drift do E3 + des-burocratizar o Cockpit *(rodada de UX; o owner já engajou)*
Detalhe completo em `docs/plans/2026-07-23_canvas-tarefas/NEXT-e3-drift-ux-prompt.md`. Resumo: o **E3 · Canvas UI** da visão tinha zonas `Foco/Vetor/Microversos/ACERVO/PERSONAS/Gaps/Artefatos/COLHEITA/Next` alimentadas pelo **E4 (Curador)**, mas F1 (MVP) cortou pra subconjunto e F2 pôs uma **ilha bolt-on** (`canvas-curador.js`) no lugar das zonas integradas — nenhuma fase assumiu terminar o E3. Fazer: (a) **des-burocratizar** (microverso vira dropdown dos 13 reais via novo forward `GET /api/canvas/microversos`; `vetor`/`intent_type`/`shape` como chips; colapsar método; liderar com a frase); (b) **terminar o E3** (Acervo Aplicado + Personas + skills sugeridas como zonas de 1ª classe DENTRO do Cockpit `renderCockpit` em `static/canvas-tarefas.js`~:187, alimentadas pelo Curador); (c) decidir **skill-usage de domínio** (a sessão lançada *usa* as skills sugeridas?). Owner travou "des-burocratizar" e inclinou a **dobrar pra incluir reuso** — reconfirmar no brainstorming.

### Fase C2 · F4 Colheita & canonização *(unidade E8 — fecha o ciclo de crescimento)*
Charter em `F4-CHARTER.md`. Fazer: **bandeja de colheita** (durante a execução, `promotion_candidates` + artefatos intermediários acumulam; cada card: destino microverso/nature, gate risk/trust, diff sob demanda); **checkout de fechamento** = 1 HITL em lote (aprovar tudo / revisar item a item / rejeitar) via `acervoctl new-object/commit-write` (propose-then-approve, ADR-022; risk gate perene/persona/macro=DRAFT-first; trust gate web=untrusted); **fable-judge no checkout** (verificação por execução/diff, distinta dos bounds de verify ao vivo do F3); **canvas → receita** (clean-portable EX-58: remove instância, preserva estrutura → acervo nature `templates`/`workflows` OKF v0.2 → **galeria de receitas no Hangar**; iniciar sala a partir de receita pré-preenche o canvas). **Graduação da memória viva de capacidades** F2 (off-trail cache → `_meta/capabilities.json` canônico). **Gate F4:** 1 sala real fechada com colheita gravada (log.md do container) + 1 receita canonizada + nova sala iniciada a partir dessa receita. **NUNCA auto-commit de promoção; receita sem dados de instância nem segredos.**

### Fase C3 · F5 Polish & GA *(fecha o gate T16 + audita + entrega)*
Charter em `F5-CHARTER.md`. Fazer: **(prioridade) calibração das skills `excrtx-conduct-loop`/`excrtx-conduct-bounds`** para FECHAR o gate T16 ao vivo — hoje o agente lançado *narra* o método em vez de escrever `conduct.jsonl` (sem cards sala automáticos); calibrar via dogfood: forçar o append shell de `conduct.jsonl`, proibir narrar a fase, alargar o grep anti-narração pra PT-BR (`"Classificação:"/"Definição de pronto:"`); re-rodar o gate ao vivo até sair ≥1 card sala + Draft-First AUTH de uma sessão REAL conduzida. Depois: **a11y** (teclado na fila de gaps + checkout, foco visível, padrão Calm Console); **i18n** (`static/i18n.js`, PT-BR primário + chaves EN, MOD-006); **calibração EX** (skills novas c/ EX-ID + cenário dogfood; `.dogfood/scenarios/EX-60.yaml`/`EX-61.yaml` que ficaram pendentes; `calibrate-hermes.sh`); **docs** (FEATURES.md + README WebUI + provision/hermes-webui/README + `sources.lock.yaml` re-pinado no SHA estável + UPSTREAM-SYNC.md); **auditoria interativa** (skill `interactive-audit` G0–G5, persona executivo não-técnico, jornada completa, backlog de achados triado); **fechamento COLLAB** (change record concluído, contrato status `ativo`, **#130 fechada com sumário**). **Gate F5/GA:** dogfood PASS + auditoria GO + provisionamento limpo (`EXOCORTEX_ENABLE_HERMES_WEBUI=1 bash setup.sh` → Canvas funcional). Achado da auditoria que exija design volta como issue-filha, não scope creep.

## MÉTODO POR FASE (repita o que entregou F0–F3)
Para CADA fase (C1, C2, C3): `superpowers:brainstorming` (alinhar escopo/decisões com o owner via AskUserQuestion) → **spec/charter→plano** → **workflow de design multiagente** (leitores paralelos do código REAL → 2–3 arquiteturas → síntese com TABELA DE DECISÕES DO OWNER → crítica adversarial) → `superpowers:writing-plans` → `PLANO.md` (TDD bite-sized, código completo, prova EX-49 por tarefa, Global Constraints do 00-INDEX verbatim + emenda do owner sobre regras 3/4) → **revisão adversarial do plano vs código REAL + re-verify do fold** → **SDD via Workflow** (sequencial impl→review→(fix) por tarefa, worktree isolada, **NUNCA 2 implementers na mesma branch**) → **revisão de branch inteira no modelo mais capaz** → **fix wave única** → **merge `--no-ff` → mains + push (owner-gated)** → **gate ao vivo** (smoke DeepSeek isolado + Playwright). Atualizar a memória `[[canvas-tarefas-meta-issue-2026-07-23]]` ao fim de cada fase.

## GOTCHAS OPERACIONAIS VINCULANTES
- **Zona quente (NÃO tocar):** `static/{ui,messages,sessions,panels,boot}.js`, `style.css`, `index.html`. **`routes.py` = 0 linhas** (endpoints por **forward** em `api/canvas_tarefas.py`). Editáveis (fork-owned, MOD): `static/canvas-*.js`/`.css`, `canvas-dev.html`, `api/canvas_*.py`, skills/SOUL no exocortex, `.harness/` no umbrella. **Emenda do owner (2026-07-26):** teto de "8 linhas" do routes.py e "zero deps" relaxados — invariante real = **não perturbar o upstream nesquena** (dep Python pequeno OK sem build; `zero build step` PERMANECE; nada de dep JS/npm).
- **Checkout compartilhado (owner roda sessões paralelas nos MESMOS checkouts):** SEMPRE worktree isolada em scratchpad; branch nova por fase (`collab/canvas-<slug>`); verificar a branch no **mesmo comando composto** do commit; **NUNCA `git add -A`** (paths explícitos). **Merge via worktree DETACHED no tip do ORIGIN** (`git worktree add --detach <wt> origin/<branch>` → merge `--no-ff` → `push origin HEAD:<branch>`) — os main checkouts podem ter divergido/ter WIP do owner (no F3 o exo estava 7 commits atrás; umbrella dirty). Ver `[[checkouts-compartilhados-sessoes-paralelas]]`.
- **Runner (hermético, sem chave):** `cd <worktree> && /home/elder/projetos/projetob/hermes-webui/.venv/bin/python -m pytest <arquivos> -q` (PYTHONPATH=worktree). Seams: `SALA_ENABLE`, `_INJECTED`, `CANVAS_LLM_CMD`, `CURADOR_*`. Lint de contrato: `UMBRELLA_ROOT=<umbrella-wt>`. Skills keyless: `python3 scripts/skill_judge.py --skill <n> --d1-only`; `compile_soul.py` compila `compiled_rules` → `SOUL_SEED.md` (⚠ usar **YAML block scalar `|`** em compiled_rules que contenham aspas/`printf`, senão o regex do compile_soul TRUNCA — bug C-S1). Playwright via `mcp__playwright__*`.
- **Smoke DeepSeek ao vivo (isolado):** `HERMES_HOME` = `cp ~/.hermes/config.yaml` + override `model.provider=deepseek` + `model.default=deepseek-v4-pro` + **`model.base_url=https://api.deepseek.com/v1`** + **`model.api_mode=openai_chat_completions`** (⚠ sem sobrescrever o `model.base_url` ele herda a **Zen** → **401 com cara de erro Anthropic**) + `providers.deepseek.base_url` + env `DEEPSEEK_API_KEY` (de `databrain/.env`). `SOUL.md`=`SOUL_SEED.md` compilado; `ACERVO` isolado = `global/tools/harness`+`global/templates` do acervo real. Server: `server.py` `HERMES_WEBUI_PORT=8792 SALA_ENABLE=1` + venv do hermes-agent + `PYTHONPATH=<worktree>`. **Prod :8787 (pid antes/depois) + acervo real INTOCADOS.** Navegador: `http://127.0.0.1:8792/static/canvas-dev.html`; a Sala aparece no **Cockpit** (clicar num card/lançar), no fim da página.
- **Governança COLLAB 3-repos:** tocar contrato SSE/canvas OU adicionar endpoint/evento = COLLAB (contrato + change-record no **umbrella**; MOD em `EXOCRTX_MODIFICATIONS.md` no **fork**; ADR/skills no **exocortex** — commits SEPARADOS por repo). Aditivo é seguro; rename/remoção de superfície (a)–(g) é breaking → novo registro.
- **Alimentar-se do que existe, não reinventar:** reuso do acervo = Curador (`sugerir_itens`/`buscar_acervo`, F2 shippado); paralelismo/kanban = `api/kanban_bridge.py`; HITL = `api/clarify.py` + `route_approvals`; harness v0.4 (`canvas_schema.py`, `register_task_from_canvas.py`, templates). Skills de MÉTODO (`excrtx-conduct-*`) ≠ skills de DOMÍNIO (sugeridas pelo Curador).

## GATES DO OWNER (não improvise; peça)
Owner-gated SEMPRE: cada merge `--no-ff` → mains + push; cada gate ao vivo (smoke DeepSeek + prod-isolation). Decisões de design/escopo por fase via AskUserQuestion. As 10 provas P1–P11 do meta issue e os guardrails do método (Draft-First EX-08, risk/trust gates, sharing firewall, anti-slop, `.quarantine` invisível, EX-49) valem em toda fase.

**Comece: leia a memória + o meta issue (E1–E10, P1–P11) + os charters F4/F5 + os prompts NEXT-e3-drift-ux e este; monte o mapa de conclusão com as 3 fases; e proponha a sequência + a Fase C1 (brainstorming/escopo) ANTES de codar.**
