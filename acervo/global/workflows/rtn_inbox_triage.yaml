routine_id: rtn_inbox_triage
name: "Triagem e promoção de inbox"
vector: manutencao
status: active
persona: arquivista

scope:
  microversos: ["*"]
  tasks: []
  artifacts: []

objective: "Triar itens de _inbox/ (e raw/ com >7 dias sem promoção) conforme 08 §1: resolver escopo (sem escopo após 1 pergunta → permanece no inbox com nota); classificar a confiança da fonte (trust gate: web/email/documentos de terceiros → sempre untrusted); extrair candidatos — fatos→knowledge, compromissos→intention, decisões→decision, eventos→episode, pessoas/orgs→entity (após checar aliases) — via `acervoctl new-object --draft` com Tier 0+1 completos e sources apontando o item original. PROPOR, nunca commitar/mover autonomamente: cada candidato nasce status: draft e entra como item no digest de manutenção; o commit (draft → active, original → raw/ do escopo) só ocorre após aprovação explícita do executivo. Junk → recomendar descarte com 1 linha no journal."
triggers:
  - "hermes cron: 0 7 * * 1"
  - "pedido explícito do executivo"
outputs:
  - "Drafts propostos via acervoctl new-object --draft (status: draft, um por candidato extraído)"
  - "Itens no digest de manutenção: 1 linha + 1 pergunta por draft/descarte aguardando aprovação"
  - "Journal: 1 linha por descarte recomendado (auditabilidade sem storage)"

permissions:
  # false = nenhuma promoção/movimentação autônoma de memória ativa.
  # O canal permitido de proposta é a criação de objetos status: draft
  # (acervoctl new-object --draft) + item no digest; o commit-on-approval
  # acontece na sessão com o executivo, nunca nesta rotina (08 §§1-2).
  can_modify_acervo: false
  can_create_reports: true
  can_publish: false
  requires_user_approval_for_external_action: true

state_path: state.json
log_path: log.md
created_at: "2026-06-15"
updated_at: "2026-07-05"
