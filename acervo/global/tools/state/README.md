# state/

Diretório de estado local (runtime) das tools globais — casa do manifesto
`acervo_hindsight_index.json` mantido por `acervo_hindsight_index.py`
(ver `../acervo-hindsight-indexer-spec.md`). Todo conteúdo aqui é **dado
derivado e mutável**: pode ser apagado com segurança e é reconstruído no
próximo `index`/`reindex`. Não é seed do repositório — os `*.json` são
ignorados pelo git; apenas este README e o `.gitkeep` são versionados.
